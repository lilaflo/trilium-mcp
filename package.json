{
  "name": "trilium-mcp-server",
  "version": "1.0.0",
  "description": "MCP server for Trilium Notes integration",
  "type": "module",
  "main": "mcp-trilium-server.ts",
  "scripts": {
    "start": "node mcp-trilium-server.ts",
    "dev": "node --watch mcp-trilium-server.ts",
    "build": "echo \"No build step required for TypeScript\"",
    "lint": "echo \"Add linting script here\"",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "mcp",
    "trilium",
    "notes",
    "server"
  ],
  "author": "",
  "license": "MIT",
  "packageManager": "pnpm@10.17.1",
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.18.2",
    "cors": "^2.8.5",
    "dotenv": "^17.2.2",
    "express": "^5.1.0",
    "node-fetch": "^3.3.2",
    "zod": "^3.25.76",
    "zod-to-json-schema": "^3.24.6"
  },
  "devDependencies": {
    "@flydotio/dockerfile": "^0.7.10",
    "@types/node": "^24.5.2"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
